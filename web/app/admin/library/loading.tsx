import { AppBoot } from "@/components/app-boot";

export default function AdminLibraryLoading() {
  return (
    <AppBoot
      label="Opening institution controls"
      detail="Loading catalog folders, documents, and publishing state"
    />
  );
}
