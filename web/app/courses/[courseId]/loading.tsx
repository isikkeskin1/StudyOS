import { AppBoot } from "@/components/app-boot";

export default function CourseLoading() {
  return (
    <AppBoot
      label="Opening course workspace"
      detail="Loading sources, mastery, practice, and exam context"
    />
  );
}
