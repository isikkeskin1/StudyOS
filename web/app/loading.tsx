import { AppBoot } from "@/components/app-boot";

export default function Loading() {
  return (
    <AppBoot
      label="Opening StudyOS"
      detail="Preparing the next view without dropping your context"
    />
  );
}
