import { AppBoot } from "@/components/app-boot";

export default function LibraryLoading() {
  return (
    <AppBoot
      label="Opening institutional library"
      detail="Indexing folders and course files"
    />
  );
}
