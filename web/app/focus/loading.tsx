import { AppBoot } from "@/components/app-boot";

export default function FocusLoading() {
  return (
    <AppBoot
      label="Entering Focus"
      detail="Locking onto the active study block"
    />
  );
}
